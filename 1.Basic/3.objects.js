let person = {
    name: 'Borshon',
    age: 22
}

// object properties can be changed into 2 ways

// Dot notation
person.name = 'Saha';
console.log(person.name);

// Bracket notation
person['age'] = 23;
console.log(person.age);
