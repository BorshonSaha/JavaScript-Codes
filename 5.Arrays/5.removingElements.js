const numbers = [1, 2, 3, 4, 5, 6, 7];

// remove from end
numbers.pop();
console.log(numbers);

// remove from beginning
numbers.shift();
console.log(numbers);

// remove from desired position
numbers.splice(2, 1);
console.log(numbers);